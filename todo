Create a program that listens for input, and generates a random response to that input in python [DONE]

Create a program that starts a conversation and uses a list of questions to collect data from inputs in python

nltk for natural language processing